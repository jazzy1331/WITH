<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bitrebels.com.with">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_with_icon_nav_bar_distinct_name"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_with_icon_nav_bar_distinct_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Alerts"
            android:label="Recent Alerts"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ReportProblem"
            android:label="Report Issue" />
        <activity
            android:name=".IssuesAroundMe"
            android:label="Issues Around Me" />
        <activity
            android:name=".ShelterFinder"
            android:label="Shelter Finder" />
        <activity
            android:name=".Blizzard"
            android:label="Blizzard Checklist" />
        <activity
            android:name=".Earthquake"
            android:label="Earthquake Checklist" />
        <activity
            android:name=".Tornado"
            android:label="Tornado Checklist" />
        <activity
            android:name=".Hurricane"
            android:label="Hurricane Checklist" />
        <activity
            android:name=".Flood"
            android:label="Flood Checklist" />
        <activity
            android:name=".ZipCodeActivity"
            android:label="Setup"></activity>
    </application>

</manifest>